# Configuração para HTTPS (comentado até implementar SSL)
# server {
#     listen 80;
#     server_name localhost;
#     # Redireciona todo HTTP para HTTPS
#     return 301 https://$host$request_uri;
# }

# Configuração principal
server {
    listen 80;
    server_name localhost;

    # Bitbook API
    location / {
        proxy_pass http://bitbook-api:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
    }

    # phpMyAdmin
    location /admin {
        proxy_pass http://phpmyadmin:80;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;

        # Configurações específicas para o phpMyAdmin
        client_max_body_size 300M;
        proxy_read_timeout 300;
        proxy_connect_timeout 300;
        proxy_send_timeout 300;
    }
}

# Bittopics API (futuro)
# server {
#     listen 443 ssl;
#     server_name localhost;
#
#     # Configuração SSL
#     ssl_certificate /etc/nginx/ssl/bitplus.crt;
#     ssl_certificate_key /etc/nginx/ssl/bitplus.key;
#     ssl_protocols TLSv1.2 TLSv1.3;
#     ssl_ciphers HIGH:!aNULL:!MD5;
#
#     location / {
#         proxy_pass http://bittopics-api:3000;
#         proxy_http_version 1.1;
#         proxy_set_header Upgrade $http_upgrade;
#         proxy_set_header Connection 'upgrade';
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#         proxy_cache_bypass $http_upgrade;
#     }
# }

# Bittrainers API (futuro)
# server {
#     listen 443 ssl;
#     server_name localhost;
#
#     # Configuração SSL
#     ssl_certificate /etc/nginx/ssl/bitplus.crt;
#     ssl_certificate_key /etc/nginx/ssl/bitplus.key;
#     ssl_protocols TLSv1.2 TLSv1.3;
#     ssl_ciphers HIGH:!aNULL:!MD5;
#
#     location / {
#         proxy_pass http://bittrainers-api:3000;
#         proxy_http_version 1.1;
#         proxy_set_header Upgrade $http_upgrade;
#         proxy_set_header Connection 'upgrade';
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#         proxy_cache_bypass $http_upgrade;
#     }
# }

# Configurações de log
access_log /var/log/nginx/access.log;
error_log /var/log/nginx/error.log;

# Configurações de timeout
proxy_connect_timeout 60s;
proxy_send_timeout 60s;